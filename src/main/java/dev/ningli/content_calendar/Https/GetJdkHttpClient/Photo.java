package dev.ningli.content_calendar.Https.GetJdkHttpClient;

public record Photo (String albumId,
                     Integer id,
                     String title,
                     String url,
                     String thumbnailUrl){
}
